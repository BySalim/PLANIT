feat(infra): beta = cloudflare tunnel sur la vm (adr-0015)

Koyeb (free tier) est passe payant comme Railway avant lui. Pivot : au lieu
de relouer un host, on expose la VM self-host deja prouvee via Cloudflare
Tunnel (cloudflared), sans ouvrir de port (connexion sortante seule).

- adr-0015 reecrit (renomme -cloudflare-tunnel-vm) : beta = VM + tunnel,
  drop Neon/Koyeb/Vercel. Rideau = Cloudflare Access (<=50 users).
- compose.prod : service cloudflared en profil opt-in `tunnel` (routage
  hostname -> https://caddy:443, No-TLS-Verify, Host=PLANIT_DOMAIN).
- runbook beta-cloud.md -> beta-tunnel.md (reecrit) ; suppression
  deploy-beta.yml (la beta reutilise le CD VM, pas de deploy cloud).
- revert pre-PR4 de next.config.ts (prod connect-src 'self') et middleware.ts
  (retrait basic-auth) : same-origin via tunnel -> aucune modif CSP/auth ;
  supprime aussi le ws:// qui avait declenche Semgrep.
- seed-data SEED_PASSWORD conserve. .env.prod.example : CLOUDFLARE_TUNNEL_TOKEN.
- maj deploy.md, vm-self-host.md, spec, tech-debt ; journal du pivot.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
