# Tech Debt PLANIT

> Dette technique tracée par jalon-cible. Les items **résolus** lors de la rétro
> Vague 01 (console.log, exception filter, rate limit, redacter logger, CORS partagé,
> etc.) sont retirés. Quand une dette est livrée, supprimer la ligne et noter le
> commit dans `docs/agent-journal/`.

---

## Observabilité (ADR-0009 — Phases 1-2 ACCEPTÉES le 2026-06-08, en cours de livraison V04+)

> Phase 0 livrée en V02 (error boundaries frontend + readiness probe). **Phases 1 & 2
> activées le 2026-06-08** (cf. [ADR-0009 §Mise à jour](architecture/adr/0009-observabilite-strategie.md)) :
> outillage tranché (requestId in-house, Sentry SaaS, prom-client+Prometheus+Grafana,
> Dozzle, Uptime Kuma), dépendances `prom-client`/`@sentry/*` validées. Les lignes
> ci-dessous se **clôturent au fil de la livraison** (profil compose `observability`).

| ID            | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Impact                                                                                                                                            | Priorité |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| TD-OBS-SENTRY | Sentry **câblé dormant** (back + web, no-op sans DSN). ✅ **Câblage infra fait** (2026-06-09) : `docker-compose.prod.yml` transmet `SENTRY_DSN` (backend + Next SSR) et `NEXT_PUBLIC_SENTRY_DSN` (build arg web), `build-images.yml` injecte le DSN navigateur au build (var repo `NEXT_PUBLIC_SENTRY_DSN`), et le `connect-src` CSP dérive l'hôte d'ingestion du DSN ([next.config.ts](../apps/web/next.config.ts)). **Reste (op Salim)** : poser la var repo `NEXT_PUBLIC_SENTRY_DSN` + `SENTRY_DSN` dans `.env.prod`, rebuild l'image web, tester la capture. **Reste (chantier)** : upload **source-maps** en CI (`withSentryConfig` + `SENTRY_AUTH_TOKEN`). | Tant qu'aucun DSN n'est posé, les erreurs ne sont pas agrégées/alertées à distance ; sans source-maps, les stack-traces client restent minifiées. | Moyenne  |
| TD-OBS-LOGS   | Pas d'**agrégateur/rétention** de logs (Dozzle ✅ donne la lecture web ; recherche/rétention longue durée = SaaS plus tard)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Rétention limitée à la vie du conteneur ; pas de recherche historique                                                                             | Faible   |

> **Livré le 2026-06-08** (retiré de la liste) : `TD-OBS-REQID` (requestId + `X-Request-Id`),
> `TD-OBS-UPTIME` / `TD-OBS-HEALTH` (Uptime Kuma sur `/api/health/ready` + alerte backup),
> `TD-OBS-METRIC` (`/api/metrics` prom-client + Prometheus + Grafana golden-signals), `TD-OBS-SINK`
> (error boundaries front câblées sur `captureException`, dormant). Profil compose `observability`.
> Cf. [observabilite.md §5](observabilite.md) et le journal du 2026-06-08.

---

## Vague 02 — Auth + RBAC + conflits salles

Tout ce qui dépend de l'arrivée de l'authentification, du modèle `User` enrichi
et des guards de rôles.

| ID                 | Description                                                                                                                                         | Impact                                                                                                                                                               | Priorité |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| TD-003             | `MailService` stub — pas de provider email réel                                                                                                     | Pas d'emails transactionnels (reset password V02 bloqué)                                                                                                             | Moyenne  |
| TD-006             | `prisma.seed` défini dans `package.json#prisma` (déprécié)                                                                                          | Warning Prisma 7, migration requise vers `prisma.config.ts`                                                                                                          | Faible   |
| TD-008             | Tests backend (`apps/backend/test/`) non typecheckés par `tsc`                                                                                      | SWC strippe les types sans les vérifier — erreurs de type non détectées dans les specs                                                                               | Faible   |
| TD-009             | LOT 2 (R.12) — undo-redo des séances sur `<PlanningGrid>`                                                                                           | Drag/resize/copier-coller livrés (V1) ; historique undo/redo toolbar reste V02                                                                                       | Moyenne  |
| TD-010             | LOT 2 (R.7/R.8) — listes hardcodées dans `apps/web/src/lib/seed-refs.ts`                                                                            | Création/édition séance limitée aux IDs seed ; impossible d'ajouter une classe/module sans rebuild                                                                   | Haute    |
| TD-011             | LOT 2 — modes de vue Classe/Salle/Prof désactivés dans `<ViewModeTabs>`                                                                             | Vue planning limitée au mode Classique V1                                                                                                                            | Moyenne  |
| TD-014             | LOT 2 — recherche topbar non branchée (`<input>` visuel uniquement)                                                                                 | Le RP ne peut pas chercher module/prof/salle — câbler sur un service de recherche                                                                                    | Moyenne  |
| TD-016             | LOT 2 — jours fériés hardcodés dans `apps/web/src/lib/holidays.ts`                                                                                  | Pas de service backend — à exposer via API + admin UI                                                                                                                | Moyenne  |
| TD-017             | LOT 2 — vue "Jour" désactivée dans `<ViewScopeToggle>`                                                                                              | Affichage day-by-day du planning à câbler                                                                                                                            | Faible   |
| TD-018             | LOT 2 — `module.color` non exposé par le contrat                                                                                                    | Couleur déterminée par hash module.id V1, palette change si la seed bouge. Ajouter `color` à `ModuleRef`                                                             | Moyenne  |
| TD-019             | LOT 2 — sélecteur classe + exporter = stubs disabled                                                                                                | Pas de filtrage par classe UI, pas d'export. Undo/redo livré en LOT 4 (PR2)                                                                                          | Moyenne  |
| TD-031             | LOT 4 (I.5) — paste hors undo                                                                                                                       | Ctrl+V crée des séances via POST mais aucun mécanisme de delete V2 pour réverter. Tracer l'add d'un DELETE /api/v2/sessions/:id en V03 pour rendre paste undoable    | Moyenne  |
| TD-032             | LOT 4 (I.1 / I.2) — création depuis slot vide hors undo                                                                                             | Identique TD-031 : la création via bouton « + » ou drag-select empty passe par POST sans undo. Nécessite delete mutation V2                                          | Moyenne  |
| TD-V03-PAGINATION  | LOT 5 V03 — pas de pagination sur `/etudiants` et `/suivi-modules`                                                                                  | OK avec la seed V03 (<20 items). Activer pagination backend + UI quand la volumétrie monte à >100 (cohérent avec pattern enseignants)                                | Faible   |
| TD-V03-CLASSE-COL  | LOT 5 V03 — page Étudiants n'affiche pas la dernière classe par ligne                                                                               | `GET /api/etudiants` ne renvoie pas `lastClasseCode`. Soit l'ajouter au DTO (Oumar), soit accepter le drawer pour cette info (V1 actuel)                             | Faible   |
| TD-020             | LOT 2 — compteurs Topbar hardcodés (3 conflits / 5 demandes / 3 notifs)                                                                             | Demo V1 sans backend — brancher sur `useConflictsQuery / useDemandsQuery / useNotifsQuery`                                                                           | Moyenne  |
| TD-021             | LOT 2 (R.12) — pas de validation conflit au collage / resize                                                                                        | Même limite V1-D4 que le drag : POST/PUT sans check salle/prof — brancher sur le service conflits V02                                                                | Moyenne  |
| TD-022             | `useCurrentTeacher()` et `useCurrentStudent()` retournent un acteur hardcodé (V1, pas d'auth)                                                       | Le `userId` envoyé au WebSocket et le filtre `teacherId` dépendent de constantes — remplacer par contexte auth                                                       | Haute    |
| TD-023             | LOT 3 — `<Sidebar>` hardcodée RP (nav + profil Aïssatou Diallo)                                                                                     | L'enseignant voit la nav RP complète (Conflits, Demandes, Étudiants, Salles). Paramétrer par rôle (TD-SIDEBAR)                                                       | Moyenne  |
| TD-024             | LOT 3 — `<Topbar>` : bouton "Demandes" (InboxIcon) visible côté enseignant                                                                          | RP-spécifique mais affiché partout. Ajouter prop `variant` ou contexte auth                                                                                          | Moyenne  |
| TD-025             | LOT 3 — Item "Planning" de la sidebar pointe vers `/rp` même côté enseignant                                                                        | L'enseignant clique sur "Planning" et atterrit sur la vue RP. Résoudre dynamiquement selon rôle (TD-023)                                                             | Moyenne  |
| TD-026             | LOT 3 — Pas de tests unitaires sur les nouveaux composants/hooks enseignant                                                                         | Couverture 0% sur `HeroCurrentSession`, `SessionsTodayList`, `SessionDetailView`, `useRealtimeSessions`, etc.                                                        | Moyenne  |
| FACTOR-PAGES       | Factoriser `/etudiant/page.tsx` et `/enseignant/page.tsx` quasi-identiques                                                                          | Duplication de logique d'accueil — attend `useCurrentActor()` (TD-022) pour mutualiser                                                                               | Moyenne  |
| FUSION-PLANNING    | Fusion `<PlanningGrid>` (RP, édition) vs `<WeekTimeline>` (Enseignant/Étudiant, lecture)                                                            | Deux composants distincts V1 — décision de fusion ou séparation actée par ADR à écrire début V02                                                                     | Moyenne  |
| REFACTOR-GRID      | Refactoriser `<PlanningGrid>` (547 lignes) vers ≤350                                                                                                | Composant trop gros — extraire les handlers drag/resize/paste, possiblement un `useGridInteractions` hook                                                            | Moyenne  |
| REPOSITORY-PATTERN | Extraire `SeanceRepository` du `SeanceService` (Prisma direct en V1)                                                                                | À faire conjointement avec `AuthRepository` V02 pour éviter deux refacto consécutives                                                                                | Faible   |
| SIDEBAR-ROLE       | Paramétrer `<Sidebar>` et `<Topbar>` par rôle (prop `variant` ou contexte auth)                                                                     | Aujourd'hui hardcodé RP — bloque l'expérience enseignant/étudiant web                                                                                                | Moyenne  |
| MIDDLEWARE-RBAC    | Middleware Next.js (`apps/web/src/middleware.ts`) pour redirections selon rôle                                                                      | Pas de garde-fou web — un user peut taper `/rp` même non-RP                                                                                                          | Haute    |
| WS-AUTH            | Sécuriser le handshake WebSocket via JWT (fix faille V1 documentée ADR-0004)                                                                        | Aujourd'hui `userId` envoyé par le client sans vérification — un attaquant peut s'abonner aux events d'autrui                                                        | Haute    |
| TD-029             | LOT 3 — Schéma Prisma **dual** V01+V02 : colonnes legacy `Seance.type/status/classeId/teacherId` à supprimer après bascule complète du web vers V02 | Migration `DROP COLUMN` à écrire en fin de LOT 3 (Oumy). Idem `Module.name` → migré vers `libelle`. Le service V01 et /api/sessions V01 doivent disparaître d'abord. | Moyenne  |
| TD-030             | LOT 3 — Suppression du service V01 (`/api/sessions`, `seance.service.ts`) après bascule complète du web vers `/api/v2/sessions`                     | LOT 2 V02 a livré les endpoints V02 en parallèle ; LOT 3 bascule le web. Une fois 100% V02, retirer le module V01 + tests V01 + colonnes legacy (TD-029).            | Haute    |

---

## Vague 03+ — AC enrichi, notifications, exports

| ID                          | Description                                                                                                                                                                                                                                                                                                                                                                | Impact                                                                                                                                                                                                                                                                                                           | Priorité | Vague cible |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| WS-SCALE                    | Adapter Redis `@socket.io/redis-adapter` quand on passe à 2+ instances backend                                                                                                                                                                                                                                                                                             | Aujourd'hui 1 process = OK ; en multi-process, un client connecté au réplica A ne reçoit pas les events de B                                                                                                                                                                                                     | Moyenne  | V03         |
| LUCIDE-ICONS                | TD-013 — Migrer `packages/ui/src/icons/` inline SVG vers `lucide-react`                                                                                                                                                                                                                                                                                                    | Maintenance manuelle des icônes — standardisation via Lucide                                                                                                                                                                                                                                                     | Faible   | V03         |
| TD-007                      | `playwright install chromium` non exécuté localement                                                                                                                                                                                                                                                                                                                       | Tests e2e non lançables hors CI                                                                                                                                                                                                                                                                                  | Moyenne  | V03         |
| TD-V03-CLASSEID             | Drop de `User.classeId` (FK directe étudiant→classe) au profit de `Inscription` (ADR-0011 §4). Conservée en transition par la migration LOT 0.4                                                                                                                                                                                                                            | Double source de vérité temporaire ; le planning par `studentId` doit basculer sur `Inscription`. Drop quand tous les consommateurs sont migrés (V03 si la donnée seed est propre, sinon V04)                                                                                                                    | Moyenne  | V03         |
| TD-V03-SUIVI-PERF           | Agrégation `heuresFaites` du suivi calculée à chaque GET (ADR-0012 §Option D). Vue matérialisée en réserve                                                                                                                                                                                                                                                                 | Acceptable à la volumétrie actuelle ; à mesurer en multi-classes au LOT 2 B.5. Si lent → vue matérialisée Postgres                                                                                                                                                                                               | Faible   | V03         |
| TD-V03-SUIVI-LAZY           | `GET /api/suivi-modules` matérialise paresseusement les lignes `SuiviModule` manquantes (effet de bord idempotent sur un GET). LOT 2 B.5                                                                                                                                                                                                                                   | Garde le suivi aligné sur la version après « Composer » sans hook ; à revisiter avec TD-V03-SUIVI-PERF (créer la ligne à l'ajout de module, ou via vue matérialisée)                                                                                                                                             | Faible   | V03         |
| TD-V03-SUIVI-ANNEE          | `resolveScopeClasseIds` (branche RP/Direction, `suivi-modules.service.ts`) est désormais **scopée école + owner RP** (V05 LOT 6 / ADR-0022), corrigeant la fuite cross-école. **Reste** : aucun filtre d'**année** — la liste renvoie les classes de toutes les années de l'école. Le pivot enseignant filtre déjà `EN_COURS` (F3).                                        | Un RP/Direction voit potentiellement le suivi de classes d'années clôturées dans la liste globale (plus de fuite cross-école). Restreindre la branche à l'année `EN_COURS` (cohérent avec F3).                                                                                                                   | Faible   | V06         |
| TD-V03-SUIVI-CONTRACT-CUID  | F6 reporté — `packages/contracts/src/academic/index.ts` (~l.403) : `classeId: z.string()` du DTO query suivi devrait être `cuid` pour s'aligner sur le reste des DTO suivi (idem front `apps/web/src/lib/queries-v3.ts:347`). Décision sensible `packages/contracts/` (soft-lock + rebuild).                                                                               | Incohérence de validation mineure ; un classeId non-cuid passe la validation Zod côté query. Aligner sur `cuid` + rebuild `@planit/contracts` + front.                                                                                                                                                           | Faible   | V03         |
| TD-V05-PLANNING-V1-SCOPE    | `GET /api/sessions` (V01, `seance.service.ts > buildWeekWhere`) n'a **aucun filtre `ecoleId`** — angle mort identique à celui corrigé sur le V2. Aujourd'hui consommé uniquement par les vues consult (étudiant/enseignant) qui filtrent déjà par acteur (`studentId`/`teacherId`), donc pas de fuite exploitable en pratique. Le grid RP/Direction utilise le V2 (scopé). | Pas de fuite via les consommateurs actuels (filtre acteur). Mais un appel direct `GET /api/sessions?weekStart=` sans filtre acteur renverrait toutes les écoles. Appliquer le même scope école que le V2 (`ecoleClasseFilter`) si le V01 reste exposé, ou retirer l'endpoint quand le V2 le remplace totalement. | Moyenne  | V06         |
| TD-V05-LOT6-PROD-BACKFILL   | La migration `20260613..._v05_lot6_ownership_isolation` est **additive** (colonnes `ownerRpId` nullable). Le backfill de propriété (`ADR-0022`) n'est appliqué qu'**au reseed dev** (`seed-data.ts > backfillOwnership`). Aucun backfill prod/staging écrit.                                                                                                               | V05 n'est **pas encore livrée sur `main`** → pas d'impact prod aujourd'hui. Avant une release V05→prod avec données réelles, écrire un script de backfill (filière→owner = responsableRp ; classes/maquettes/séances dérivés ; UE/modules « 1er match ») ou repartir d'un onboarding propre.                     | Moyenne  | V05-release |
| TD-V05-LOT6-WS-TRANSFER     | `ownerRpId` est **immuable** (ADR-0022 §1). Aucun mécanisme de **transfert d'espace de travail** quand un RP quitte (ses filières/classes/maquettes/séances restent à son `ownerRpId`, invisibles des autres RP). La Direction peut réassigner le « Responsable » mais pas l'owner.                                                                                        | Un RP archivé laisse un référentiel orphelin (visible Direction, invisible des RP actifs). Ajouter une action Direction « réattribuer l'espace de travail » (réécrire `ownerRpId` en masse) si le besoin émerge.                                                                                                 | Faible   | V06+        |
| TD-V05-LOT6-SUBJ-SALLE-UNIQ | La contrainte `@@unique([ecoleId, name])` sur `Salle` s'applique aussi aux salles **subjectives** : deux RP ne peuvent pas créer une subjective de même nom dans la même école, et une subjective ne peut pas homonymer une salle normale.                                                                                                                                 | Edge case mineur (collision 409 sur un nom dupliqué entre subjectives/normales). Si gênant, scoper l'unicité par `ownerRpId` pour les subjectives (index partiel `WHERE isSubjective`).                                                                                                                          | Faible   | V06+        |

---

## Vague 04 — Mobile + WhatsApp + SMS

| ID                   | Description                                                                                                                                                                                                                                                                                                         | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priorité |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| TD-001               | `apps/mobile` squelette — Expo non configuré                                                                                                                                                                                                                                                                        | Aucun dev mobile possible                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Haute    |
| TD-002               | `apps/whatsapp-bot` squelette — Baileys non intégré                                                                                                                                                                                                                                                                 | Pas de diffusion WhatsApp                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Haute    |
| MOBILE-DEPS          | `apps/mobile` ne consomme pas encore `@planit/design-tokens` ni `@planit/utils`                                                                                                                                                                                                                                     | Une fois Expo bootstrappé, brancher les packages partagés (tokens + helpers date)                                                                                                                                                                                                                                                                                                                                                                                                       | Haute    |
| CI-MOBILE            | Pas de step build mobile en CI                                                                                                                                                                                                                                                                                      | Aucun garde-fou TS/lint pour `apps/mobile` une fois activé                                                                                                                                                                                                                                                                                                                                                                                                                              | Haute    |
| TD-V02-LOT3-C        | `SOUS_TYPE_LABEL` dupliqué entre `create-session-modal.tsx` et `session-detail-drawer.tsx`                                                                                                                                                                                                                          | Source unique de vérité manquante — à extraire dans `@planit/contracts` (libellés métier) ou un fichier UI partagé                                                                                                                                                                                                                                                                                                                                                                      | Faible   |
| TD-LH-CSP-NONCE      | Audit Lighthouse `csp-xss` score 0 — CSP actuelle utilise `'unsafe-inline'` sur `script-src`                                                                                                                                                                                                                        | **Downgrade à `warn`** depuis pré-release V02 ([lighthouserc.json](../.github/lighthouserc.json)). CSP de base posée via `next.config.ts > headers()` (default-src 'self', object-src 'none', frame-ancestors 'none', base-uri 'self') mais LH exige `'strict-dynamic'` + nonce par requête. Chantier sprint sécurité prod : middleware Next.js qui pose un nonce + `next/script` configuré strategy 'beforeInteractive' avec ce nonce                                                  | Moyenne  |
| TD-LH-CONSOLE-AUTHME | Audit Lighthouse `errors-in-console` score 0 — Chrome log les 401 fetch sur `/api/auth/me` (visite anonyme = pas de cookie)                                                                                                                                                                                         | **Downgrade à `warn`** depuis pré-release V02. Comportement légitime de l'app (`/auth/me` retourne 401 sans cookie = état attendu). Hooks de données gates sur `state.status === 'authenticated'` (`queries.ts`, `use-realtime-sessions.ts`) éliminent les fetch protégés mais le fetch initial /auth/me reste. Options pour repasser en `error` : (a) backend retourne 200 + `{user: null}` au lieu de 401 (changement contract = ADR), (b) audit LH avec cookie via `puppeteerScript` | Faible   |
| ~~TD-LH-LANDMARK~~   | ~~Audit Lighthouse `landmark-one-main` à 0 — page `/login` (cible finale du redirect LH) sans `<main>`~~                                                                                                                                                                                                            | **Résolu** (2026-05-31, pré-release V02) : `apps/web/src/app/login/layout.tsx` passe son wrapper de `<div>` à `<main>`. Les Shells RP/Enseignant/Étudiant exposaient déjà `<main>` ; restait `/login` qui devient l'URL finale après redirect quand l'audit tourne sans cookie                                                                                                                                                                                                          | —        |
| TD-LH-PERF           | Audits perf individuels du preset `lighthouse:no-pwa` sous-optimaux (`bootup-time`, `dom-size`, `mainthread-work-breakdown`, `server-response-time`, `largest-contentful-paint` 0.86, `total-byte-weight` 305 KiB, `prioritize-lcp-image`, `unused-javascript`, `render-blocking-resources`, `uses-long-cache-ttl`) | **Downgrade à `warn`** depuis pré-release V02. La **catégorie `performance` score 0.97** — largement au-dessus du seuil 0.85 — donc l'app est saine globalement. Les audits individuels mesurent des tendances strict ; consolidation prévue en sprint perf V03 (code-splitting, tree-shaking design-tokens, lazy-loading modals, cache headers Caddy en prod)                                                                                                                          | Faible   |
| TD-LH-AUTH-AUDIT     | Le job Lighthouse n'audite que `/login` (seule page publique). Depuis l'auth V02, le middleware edge redirige toute route applicative en 307 → `/login` en anonyme, ce qui faisait échouer l'audit `redirects` sur la 1ʳᵉ PR strict vers `main` (release V02)                                                       | **Workaround** (2026-06-02, release V02) : les URLs auditées (`ci.yml`) passent de `/etudiant`+`/enseignant` à `/login` — page réellement atteignable sans session, scores perf/a11y représentatifs du shell. **Limite** : on n'audite pas le contenu réel des pages connectées. Fix propre = run authentifié via `puppeteerScript` (seed user + cookie) — cf. `docs/runbooks/ci-lighthouse.md` § « Quelle URL est auditée ». Sprint perf V03+                                          | Faible   |

---

## Vague 04 — Déploiement & Qualité (conteneurisation / CI / infra)

> Dette sortie du LOT 1 (conteneurisation). Tracée pour optimisation ultérieure ;
> n'empêche pas le « Done LOT 1 » (smoke vert sur les 3 critères).

| ID                        | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Impact                                                                                                                                                                                                                                                                                                                                     | Priorité |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| TD-V04-ADMIN-PROVISIONING | LOT 8 / ADR-0017 — **Pas d'écran/endpoint admin** de provisioning. Les comptes réels initiaux sont créés par un script one-off `bootstrap-prod.ts` (1 RP + 1 AC + 1 enseignant + 1 étudiant) ; au-delà, la création passe par les **cascades existantes** (RP → AC via `managerRpId`, enseignants, classes, inscriptions email-first). Décision TL « après on verra ».                                                                                                                                                                                                                                                                                                 | Pas d'auto-service de gestion des utilisateurs (création/désactivation/reset) hors cascades RP ; rôles `ADMIN`/`SUPER_ADMIN` (déjà dans l'enum Prisma) + multi-RP non exploités. À ouvrir avec le besoin Direction/Admin (V05+). Reset mot de passe au pilote = CLI `reset-password.ts` (email différé, TD-003).                           | Moyenne  |
| TD-V04-IMG-SIZE           | Image backend runtime **646 MB** (`pnpm deploy --legacy` recopie aussi `src/` ; engines Prisma lourds). Service `migrate` = stage `build` **2.45 GB** (devDeps + sources complètes).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Pull/stockage plus lourds (GHCR, Railway, VM). Optimiser : image `migrate` dédiée slim (prisma + schema + migrations seuls), élaguer `src/` du bundle backend, n'embarquer que l'engine ciblé. À traiter avec LOT 4.4 (scan image) / LOT 5 (CD).                                                                                           | Faible   |
| TD-V04-DOCKER-CACHE       | `COPY . .` **avant** `pnpm install` invalide le cache d'install dès qu'un fichier source change. Le cache mount du store pnpm atténue (downloads réutilisés) mais l'install se ré-exécute entièrement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Builds CI plus lents que nécessaire. Optimiser via layering des manifestes (copier les `package.json` + `pnpm-lock.yaml`, `pnpm fetch`/install, puis copier la source).                                                                                                                                                                    | Faible   |
| TD-V04-WEB-COMPONENT-COV  | LOT 2 — Le gate coverage unitaire `apps/web` **exclut** les arbres de vue lourds `components/rp/**`, `components/enseignant/**`, `components/consult/**` (modals / listes / calendriers CRUD), gatés par l'**e2e Playwright** (ADR-0014 §4) et non par l'unitaire. Le gate unitaire couvre la couche logique (`lib`/`hooks`/`contexts`) + primitives (`ui`/`layout`/`planning`).                                                                                                                                                                                                                                                                                       | Pas de filet unitaire fin sur ces composants (un bug de rendu hors parcours e2e passerait). Faire monter la couverture composant par composant (tests de rendu ciblés), puis retirer les exclusions du `apps/web/vitest.config.ts` et remonter les seuils.                                                                                 | Faible   |
| TD-V04-DEPENDABOT-GEL     | LOT 4.7 — Dependabot **gelé** (décision TL, 2026-06-07) : `.github/dependabot.yml` renommé en `.disabled` (PR #75 mergée sur `develop`). Le burst initial de 8 PRs (#67→#74) a été fermé. **5 montées majeures restent à traiter** : prisma 6→7, eslint 9→10, eslint-config-next 15→16, node 24→26-alpine, typescript 5→6.                                                                                                                                                                                                                                                                                                                                             | Plus de MAJ automatique des deps/actions/images tant que gelé → les correctifs (dont sécurité) ne sont plus proposés ; suspend temporairement le « Done LOT 4 : MAJ deps/actions automatisées ». **Réactiver** = renommer `.disabled` → `.github/dependabot.yml`, puis traiter les majeures une par une (chantier dédié, CI verte par PR). | Moyenne  |
| TD-V04-BETA-EXTERNE       | ✅ **RÉSOLU** (ADR-0015, V04 LOT 5) — beta externe via **Cloudflare Tunnel sur la VM self-host** (gratuit, sans port entrant ; réutilise la stack prouvée). **Décision 2026-06-08** : les hébergeurs PaaS loués (Railway/Koyeb/Neon/Vercel) sont **définitivement abandonnés** ; on privilégie pour l'instant le **quick-tunnel sans domaine** (`*.trycloudflare.com` + `basic_auth` Caddy) — le named tunnel + domaine + Cloudflare Access restent dispo pour une URL stable plus tard. Service `cloudflared` (profil `tunnel`) + seed `SEED_PASSWORD` (5.3).                                                                                                         | Accès beta externe disponible (gratuit, sans rien louer). Procédure : `docs/runbooks/beta-tunnel.md`.                                                                                                                                                                                                                                      | Faible   |
| TD-V04-WS-BUILDARG        | ✅ **RÉSOLU** (2026-06-10, option b) : le client WS est **same-origin par défaut** — `WS_URL=''` en prod → `io()` vise `window.location`, path `/socket.io` routé vers le backend par Caddy ([api.ts](../apps/web/src/lib/api.ts), [Caddyfile.prod](../infra/caddy/Caddyfile.prod)). **Aucune URL à inliner au build** : l'image GHCR unique marche sur tout domaine (planit.local, beta, futur). `NEXT_PUBLIC_WS_URL` reste un override optionnel (build arg conservé dans `build-images.yml`, inutile de poser la var repo). Au passage, fix du fallback dev cassé (`…:3001/api` = namespace socket.io inexistant → realtime local mort en silence depuis le 31/05). | Plus d'impact. Realtime fonctionnel en local (fallback `:3001` direct) et sur VM/beta (same-origin via Caddy) dès le déploiement de ces commits.                                                                                                                                                                                           | —        |

---

## Nice-to-have (sans vague-cible)

| ID           | Description                                                                                 | Impact                                                                                                                                                                                                                                  | Priorité |
| ------------ | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| TD-012       | LOT 2 — Sidebar : drag-resize partiel vs proto                                              | Resize sidebar livré (iter. B), écarts résiduels vs `PLANIT-IA/` à affiner                                                                                                                                                              | Faible   |
| DISK-CLEANUP | Script de nettoyage `pnpm-store` + `node_modules` orphelins (incident Oumy 2026-05-22)      | 5+ GB récupérables, libère le disque dev Windows                                                                                                                                                                                        | Faible   |
| TD-031       | Réactiver le cache `turbo` sur les scripts racine (`dev`/`build`/`lint`/`typecheck`/`test`) | Aujourd'hui `pnpm -r --parallel` (Smart App Control Win11 bloque `turbo.exe` non signé). À revisiter si la lenteur des builds CI devient un problème — soit Turbo signe ses binaires, soit on isole `turbo` en step CI Linux uniquement | Faible   |

---

## Comment ajouter une dette

1. Identifier le jalon-cible (V02 / V03+ / V04 / Nice-to-have).
2. Insérer une ligne dans la bonne section avec ID stable (`TD-NNN`,
   `<DOMAIN>-<TOPIC>`, etc.).
3. Décrire l'impact en 1 ligne — pas la solution.
4. Si > 1h de chantier, créer une issue GitHub via `/tech-debt` et lier l'ID ici.
5. À la livraison, supprimer la ligne et le mentionner dans le journal d'agent.

## Comment retirer une dette

- Quand le code qui la supportait est mergé sur `develop` et que les
  tests passent.
- Ajouter une mention « Tech-debt levée : `<ID>` » dans le journal d'agent
  ou le commit message.
